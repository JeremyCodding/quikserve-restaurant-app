// src/theme.ts
export const theme = {
  colors: {
    background: "#fff",
    primary: "#36231C",
    secondaryBackground: "#F8F9FA",
    accent: "#...",
  },
  fonts: {
    // ... your font families and sizes
  },
};

// Optional: create a global style using `createGlobalStyle` from styled-components
